# Production Environment Variables Template
# Copy these to Render Dashboard → Settings → Environment Variables

# Node Environment
NODE_ENV=production
PORT=10000

# Database Configuration (Update with your Aiven/PlanetScale credentials)
DB_HOST=your-mysql-host.aivencloud.com
DB_PORT=25060
DB_USER=avnadmin
DB_PASSWORD=your_database_password_here
DB_NAME=defaultdb

# JWT Configuration (Generate a strong random secret)
JWT_SECRET=change_this_to_a_strong_random_secret_key_in_production
JWT_EXPIRES_IN=24h

# CORS Configuration (Update after frontend deployment)
CORS_ORIGIN=https://your-frontend-url.onrender.com

# Optional: Logging
LOG_LEVEL=info
